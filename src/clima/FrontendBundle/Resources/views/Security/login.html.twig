<!DOCTYPE html>
<html>
    <head>

        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="">
        <meta name="author" content="">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <title>Inicio de sesion Frontend!</title>
    </head>
            {% block stylesheets %}            
                <link href="{{ asset('bootstrap-3.3.2-dist/css/bootstrap.min.css') }}" type="text/css" rel="stylesheet" />
                <link href="{{ asset('css/signin.css') }}" type="text/css" rel="stylesheet" />
                <link href="{{ asset('csspersonalizado/styles.css') }}" type="text/css" rel="stylesheet" />
            {% endblock %}
                
    {% block body %} 
    
    <div class="container">
            <form class="form-signin" role="form" action="{{ path('login_check') }}" method="post"> 
            
               <h2 align="center">Acceso Frontend JG</h2>
               <input type="email" class="form-control" placeholder="Email address" id="username" name="_username" value="{{ last_username }}" required autofocus>
               <input type="password" class="form-control" placeholder="Password" id="password" name="_password" required>
               <button type="submit" class="btn btn-lg btn-primary btn-block">Ingresar</button>
                    
            
        </div>
    </form>
<a class="btn btn-lg btn-default btn-block" href="{{ path('registro') }}">Nuevo usuario</a>
{% if error %}
    <div class="paa">
    <div class="alert alert-danger" role="alert">{{ error.message }} : Credenciales erradas.</div>
    </div>
{% endif %}

{% endblock %}
</html>